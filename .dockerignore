# Contexto de build de la PWA (Dockerfile.web). Mantenerlo liviano y sin secretos.
node_modules
dist
dist-ssr
dev-dist
.git
.gitignore

# Secretos y locales (NUNCA al contexto de build).
.env
.env.*
*.local

# Backend: tiene su propio build (context ./server), no hace falta aquí.
server

# Ruido varios.
*.log
.data
*.tsbuildinfo
.claude
.vscode
.idea
.DS_Store
